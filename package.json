{
  "name": "focus-tracker",
  "private": true,
  "description": "",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format2": "prettier --write \"**/*.{.json,html,ts,tsx,md}\"",
    "format": "prettier --write . --ignore-unknown",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@michaeldrotar/react-focus-tracker": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.2.4",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.2.5",
    "turbo": "^1.13.2"
  }
}
